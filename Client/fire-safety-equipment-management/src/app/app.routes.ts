import { Routes } from '@angular/router';

/**
 * Application-level route table.
 *
 * The dashboard currently renders directly from the root component, so no routed pages
 * are registered yet.
 */
export const routes: Routes = [];
